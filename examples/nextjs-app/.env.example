# Villa SDK Configuration
# These are optional - config is set in app/layout.tsx
# But you can use environment variables if you prefer:

# NEXT_PUBLIC_VILLA_APP_ID=your-app-id
# NEXT_PUBLIC_VILLA_NETWORK=base-sepolia

# DEPRECATED

Agent definitions have been consolidated to `.claude/agents/`.

This directory is kept for backwards compatibility but should not be used.

**Canonical location:** `.claude/agents/`

To update an agent definition, edit the file in `.claude/agents/`.

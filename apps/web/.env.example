# Villa Web App Environment
# ==========================
# This file is app-specific. See root .env.example for all variables.
# Copy to .env.local (gitignored) or use root .env.local (inherited)

# Application
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development

# Porto ID Configuration
NEXT_PUBLIC_PORTO_ENV=stg            # stg (staging) or prod
NEXT_PUBLIC_PORTO_DIALOG_HOST=https://id.porto.sh

# API (relative URL works for same-origin, absolute for separate API)
NEXT_PUBLIC_API_URL=/api

# Unforgettable SDK (Face Recovery - Phase 2)
# NEXT_PUBLIC_UNFORGETTABLE_APP_ID=

# Map Configuration (optional, has defaults)
# NEXT_PUBLIC_TILE_SERVER_URL=https://tile.openstreetmap.org/{z}/{x}/{y}.png
# NEXT_PUBLIC_MAP_DEFAULT_CENTER_LAT=10.9333
# NEXT_PUBLIC_MAP_DEFAULT_CENTER_LNG=108.2833

# Development Settings
NEXT_PUBLIC_ENABLE_DEV_TOOLS=true
NEXT_PUBLIC_LOG_LEVEL=debug

# AI Configuration (optional, server-side only)
# ANTHROPIC_API_KEY=

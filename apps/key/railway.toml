[build]
builder = "dockerfile"
dockerfilePath = "apps/key/Dockerfile"

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 45
startCommand = "node server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
numReplicas = 1

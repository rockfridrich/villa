# Villa API Service Environment
# ==============================
# This file is app-specific. See root .env.example for all variables.
# Copy to .env (gitignored) for local development.

# Server
PORT=3001
APP_ENV=local                        # local, development, staging, production, test

# Database (PostgreSQL)
# =====================
# Local Docker development:
DATABASE_URL=postgresql://villa:villa_dev_password@localhost:5432/villa_dev

# DigitalOcean Managed PostgreSQL (staging/production):
# DATABASE_URL=postgresql://doadmin:pass@private-host:25060/villa?sslmode=require

# For serverless/high-concurrency, use pooler port (25061):
# DATABASE_POOL_URL=postgresql://doadmin:pass@private-host:25061/villa?sslmode=require

# Database Options (optional - sensible defaults provided)
# DATABASE_SSL=require               # false, true, require, prefer
# DB_MAX_CONNECTIONS=10
# DB_IDLE_TIMEOUT=20
# DB_CONNECT_TIMEOUT=10
# DB_RETRY_ATTEMPTS=3
# DB_RETRY_DELAY=1000
# DB_ENABLE_FALLBACK=true            # Enable in-memory fallback (dev only!)

# Logging
LOG_LEVEL=debug
# LOG_DB_CONFIG=true                 # Log DB config on startup (debugging)
